sources:
  - name: GatewaySDK
    handler:
      graphql:
        endpoint: https://develop.protocol.mygateway.xyz/v1/graphql
    transforms:
      - filterSchema:
          mode: bare
          filters:
            - Query.!transaction
            - Query.!transactions

customFetch: ./custom-fetch.ts

sdk:
  generateOperations:
    selectionSetDepth: 2